# GPT Exchange Reports

This directory keeps only committed WP0 documentation samples.

Live GPT information-exchange outputs are generated under:

- `reports/gpt_exchange/latest/`
- `reports/gpt_exchange/archive/`

Those live outputs are gitignored unless a future package explicitly promotes a small public fixture.
